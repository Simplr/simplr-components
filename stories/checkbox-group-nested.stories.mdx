import { html } from 'lit-html';
import '../lib/input/checkbox-group/checkbox-group.js';
import { withKnobs, withWebComponentsKnobs, Story, Preview, Meta, Props } from '@open-wc/demoing-storybook';

<Meta
  title="Input|Checkbox|Checkbox group Nested"
  decorators={[withKnobs, withWebComponentsKnobs]}
  parameters={{ component: 'simplr-checkbox-group', options: { selectedPanel: 'storybookjs/knobs/panel' } }}
/>

# Simplr Checkbox Group Nested

### Usage

```js
  import '@simplr-wc/checkbox-group';


const multipleCategoriesOptions = [
  {
    label: 'Category one',
    id: 'cat-1',
    options: [
      { label: 'Category one, option one', value: 1 },
      { label: 'Category one, option two', value: 2 },
    ],
  },
  {
    label: 'Category two',
    id: 'cat-2',
    options: [
      { label: 'Category two, option three', value: 3 },
      { label: 'Category two, option four', value: 4 },
      { label: 'Category two, option five', value: 5 },
      { label: 'Category two, option six', value: 6 },
    ],
  },
];
```

```html
<simplr-checkbox-group
  label="Single category options"
  .options="${multipleCategoriesOptions}"
  .selectedOptions=${[1, 2, 5]}
></simplr-checkbox-group>
```

<Story name="Simplr Checkbox Group Nested">
  {html`
    <simplr-checkbox-group
      label="Single category options"
      .options="${[
                    {
                      label: 'Category one',
                      id: 'cat-1',
                      options: [
                        { label: 'Category one, option one', value: 1 },
                        { label: 'Category one, option two', value: 2 },
                      ],
                    },
                    {
                      label: 'Category two',
                      id: 'cat-2',
                      options: [
                        { label: 'Category two, option three', value: 3 },
                        { label: 'Category two, option four', value: 4 },
                        { label: 'Category two, option five', value: 5 },
                        { label: 'Category two, option six', value: 6 },
                      ],
                    },
                  ]}"
      .selectedOptions=${[1,2, 5]}
    ></simplr-checkbox-group>
  `}
</Story>

<Props of="simplr-checkbox-group" />
